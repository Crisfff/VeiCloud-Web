gsap.registerPlugin(ScrollTrigger);const intro=gsap.timeline();intro.to('.intro-line',{width:180,duration:.8,ease:'power3.out'}).from('.intro-mark',{scale:.7,opacity:0,duration:.7},0).from('.intro p',{opacity:0,y:8,duration:.5},.35).to('.intro',{opacity:0,duration:.8,delay:.35,pointerEvents:'none'}).from('.nav',{y:-40,opacity:0,duration:.8},'-=.35').from('.hero-copy>*',{y:34,opacity:0,stagger:.09,duration:.8,ease:'power3.out'},'-=.65').from('.device-stage',{y:50,opacity:0,scale:.94,duration:1.1,ease:'power3.out'},'-=.9');document.addEventListener('pointermove',e=>{const glow=document.querySelector('.cursor-glow');if(glow){glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px'}});const device=document.getElementById('device');if(device){document.querySelector('.device-stage').addEventListener('pointermove',e=>{const r=e.currentTarget.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;gsap.to(device,{rotateY:x*16,rotateX:-y*12,x:x*10,y:y*7,duration:.5,ease:'power2.out'})});document.querySelector('.device-stage').addEventListener('pointerleave',()=>gsap.to(device,{rotateY:0,rotateX:0,x:0,y:0,duration:.8,ease:'elastic.out(1,.5)'}))}document.querySelectorAll('.magnetic').forEach(el=>{el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect(),x=e.clientX-r.left-r.width/2,y=e.clientY-r.top-r.height/2;gsap.to(el,{x:x*.16,y:y*.22,duration:.25})});el.addEventListener('pointerleave',()=>gsap.to(el,{x:0,y:0,duration:.6,ease:'elastic.out(1,.4)'}))});gsap.utils.toArray('.section-label,.section-title,.feature,.network-copy,.globe-shell,.plan,.download-inner').forEach(el=>gsap.from(el,{scrollTrigger:{trigger:el,start:'top 84%'},y:48,opacity:0,duration:.9,ease:'power3.out'}));gsap.to('.statement-line:first-child',{xPercent:-12,scrollTrigger:{trigger:'.statement',start:'top bottom',end:'bottom top',scrub:1}});gsap.to('.statement-line.accent',{xPercent:12,scrollTrigger:{trigger:'.statement',start:'top bottom',end:'bottom top',scrub:1}});document.querySelectorAll('[data-count]').forEach(el=>ScrollTrigger.create({trigger:el,start:'top 85%',once:true,onEnter:()=>{const target=+el.dataset.count,obj={v:0};gsap.to(obj,{v:target,duration:1.5,ease:'power3.out',onUpdate:()=>el.textContent=Math.round(obj.v)})}}));document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const t=document.querySelector(a.getAttribute('href'));if(t){e.preventDefault();t.scrollIntoView({behavior:'smooth'})}}));function bgParticles(){const c=document.getElementById('webgl');if(!c||!window.THREE)return;const scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(60,innerWidth/innerHeight,.1,100),renderer=new THREE.WebGLRenderer({canvas:c,alpha:true,antialias:true});renderer.setPixelRatio(Math.min(devicePixelRatio,1.7));renderer.setSize(innerWidth,innerHeight);camera.position.z=7;const count=900,pos=new Float32Array(count*3);for(let i=0;i<count;i++){pos[i*3]=(Math.random()-.5)*16;pos[i*3+1]=(Math.random()-.5)*10;pos[i*3+2]=(Math.random()-.5)*9}const geo=new THREE.BufferGeometry();geo.setAttribute('position',new THREE.BufferAttribute(pos,3));const mat=new THREE.PointsMaterial({color:0xff5a3d,size:.018,transparent:true,opacity:.48});const pts=new THREE.Points(geo,mat);scene.add(pts);let mx=0,my=0;addEventListener('pointermove',e=>{mx=(e.clientX/innerWidth-.5)*.35;my=(e.clientY/innerHeight-.5)*.22});function loop(){requestAnimationFrame(loop);pts.rotation.y+=.00045;pts.rotation.x+=(my-pts.rotation.x)*.015;pts.rotation.y+=(mx-pts.rotation.y)*.006;renderer.render(scene,camera)}loop();addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)})}function globe(){const c=document.getElementById('globe');if(!c||!window.THREE)return;const scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(42,1,.1,100),renderer=new THREE.WebGLRenderer({canvas:c,alpha:true,antialias:true});renderer.setPixelRatio(Math.min(devicePixelRatio,1.6));camera.position.z=4.8;const group=new THREE.Group();scene.add(group);const sphere=new THREE.Mesh(new THREE.SphereGeometry(1.6,48,48),new THREE.MeshBasicMaterial({color:0x222934,wireframe:true,transparent:true,opacity:.46}));group.add(sphere);const glow=new THREE.Mesh(new THREE.SphereGeometry(1.67,48,48),new THREE.MeshBasicMaterial({color:0xff4b2b,transparent:true,opacity:.055}));group.add(glow);const points=[];for(let i=0;i<65;i++){const phi=Math.acos(2*Math.random()-1),theta=Math.random()*Math.PI*2,x=1.62*Math.sin(phi)*Math.cos(theta),y=1.62*Math.cos(phi),z=1.62*Math.sin(phi)*Math.sin(theta);points.push(new THREE.Vector3(x,y,z))}const pgeo=new THREE.BufferGeometry().setFromPoints(points),pmat=new THREE.PointsMaterial({color:0xff6548,size:.035,transparent:true,opacity:.9});group.add(new THREE.Points(pgeo,pmat));const nodeMat=new THREE.MeshBasicMaterial({color:0x57e3a4});[[-.9,.7,1.1],[.45,1.1,.9],[1.2,.2,.8],[-.3,-1.2,1],[.8,-.8,1.1]].forEach(v=>{const n=new THREE.Mesh(new THREE.SphereGeometry(.045,16,16),nodeMat);n.position.set(...v);group.add(n)});function size(){const r=c.getBoundingClientRect();renderer.setSize(r.width,r.height,false);camera.aspect=r.width/r.height;camera.updateProjectionMatrix()}size();function loop(){requestAnimationFrame(loop);group.rotation.y+=.0022;group.rotation.x=.14;renderer.render(scene,camera)}loop();addEventListener('resize',size)}bgParticles();globe();