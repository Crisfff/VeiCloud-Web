gsap.registerPlugin(ScrollTrigger);const intro=gsap.timeline();intro.to('.intro-symbol',{scale:1,rotation:0,duration:.7,ease:'back.out(1.8)'}).to('.intro-bar',{width:190,duration:.6,ease:'power3.out'},'-=.3').to('.intro span',{opacity:1,y:0,duration:.4},'-=.25').to('.intro',{yPercent:-100,duration:.9,ease:'power4.inOut',delay:.35});gsap.from('.intro-symbol',{scale:.3,rotation:-20,duration:0});gsap.set('.intro span',{opacity:0,y:8});gsap.from('.nav',{y:-90,opacity:0,duration:1,delay:1.4,ease:'power4.out'});gsap.from('.hero-copy>*',{y:45,opacity:0,stagger:.09,duration:1,delay:1.25,ease:'power4.out'});gsap.from('.device-wrap',{y:100,rotation:10,scale:.82,opacity:0,duration:1.5,delay:1.35,ease:'expo.out'});gsap.from('.metric-card',{scale:.4,opacity:0,stagger:.15,duration:.8,delay:2.05,ease:'back.out(1.8)'});document.querySelectorAll('.reveal').forEach(el=>gsap.to(el,{opacity:1,y:0,scale:1,duration:1,ease:'power3.out',scrollTrigger:{trigger:el,start:'top 84%',once:true}}));gsap.to('.statement-line',{xPercent:-8,scrollTrigger:{trigger:'.statement',start:'top bottom',end:'bottom top',scrub:1}});gsap.to('.statement-line.accent',{xPercent:8,scrollTrigger:{trigger:'.statement',start:'top bottom',end:'bottom top',scrub:1}});const aura=document.querySelector('.cursor-aura');window.addEventListener('pointermove',e=>gsap.to(aura,{x:e.clientX,y:e.clientY,duration:.45,ease:'power3.out'}));const device=document.getElementById('device');if(device&&matchMedia('(pointer:fine)').matches){window.addEventListener('pointermove',e=>{const r=device.getBoundingClientRect(),x=(e.clientX-r.left-r.width/2)/r.width,y=(e.clientY-r.top-r.height/2)/r.height;gsap.to(device,{rotationY:x*18,rotationX:-y*14,x:x*10,y:y*8,duration:.6,ease:'power3.out'})});document.querySelector('.visual-stage').addEventListener('pointerleave',()=>gsap.to(device,{rotationY:0,rotationX:0,x:0,y:0,duration:.8,ease:'elastic.out(1,.5)'}))}document.querySelectorAll('.magnetic').forEach(el=>{el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect();gsap.to(el,{x:(e.clientX-r.left-r.width/2)*.18,y:(e.clientY-r.top-r.height/2)*.18,duration:.3})});el.addEventListener('pointerleave',()=>gsap.to(el,{x:0,y:0,duration:.6,ease:'elastic.out(1,.45)'}))});document.querySelectorAll('.feature,.price-card').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width,y=(e.clientY-r.top)/r.height;card.style.background=`radial-gradient(circle at ${x*100}% ${y*100}%,rgba(255,67,37,.12),transparent 32%),linear-gradient(145deg,rgba(255,255,255,.07),rgba(255,255,255,.018))`});card.addEventListener('pointerleave',()=>card.style.background='')});document.querySelectorAll('[data-count]').forEach(el=>ScrollTrigger.create({trigger:el,start:'top 88%',once:true,onEnter:()=>{const target=+el.dataset.count;gsap.fromTo(el,{innerText:0},{innerText:target,duration:1.5,snap:{innerText:1},ease:'power3.out'})}}));document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const id=a.getAttribute('href'),target=document.querySelector(id);if(target){e.preventDefault();target.scrollIntoView({behavior:'smooth'})}}));function initBackground(){const canvas=document.getElementById('webgl');if(!canvas||!window.THREE)return;const scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(55,innerWidth/innerHeight,.1,100),renderer=new THREE.WebGLRenderer({canvas,alpha:true,antialias:true});renderer.setPixelRatio(Math.min(devicePixelRatio,1.7));renderer.setSize(innerWidth,innerHeight);camera.position.z=7;const count=900,geo=new THREE.BufferGeometry(),pos=new Float32Array(count*3);for(let i=0;i<count;i++){pos[i*3]=(Math.random()-.5)*16;pos[i*3+1]=(Math.random()-.5)*11;pos[i*3+2]=(Math.random()-.5)*8}geo.setAttribute('position',new THREE.BufferAttribute(pos,3));const mat=new THREE.PointsMaterial({color:0xff5a3c,size:.018,transparent:true,opacity:.58,blending:THREE.AdditiveBlending});const points=new THREE.Points(geo,mat);scene.add(points);const ringGeo=new THREE.TorusGeometry(2.8,.008,10,180),ringMat=new THREE.MeshBasicMaterial({color:0xff4325,transparent:true,opacity:.18});const ring=new THREE.Mesh(ringGeo,ringMat);ring.rotation.x=1.15;ring.rotation.z=.3;scene.add(ring);let mx=0,my=0;addEventListener('pointermove',e=>{mx=(e.clientX/innerWidth-.5);my=(e.clientY/innerHeight-.5)});function tick(){requestAnimationFrame(tick);points.rotation.y+=.00045;points.rotation.x+=(my*.08-points.rotation.x)*.01;points.rotation.y+=(mx*.12-points.rotation.y)*.008;ring.rotation.z+=.0012;renderer.render(scene,camera)}tick();addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)})}function initGlobe(){const canvas=document.getElementById('globe');if(!canvas||!window.THREE)return;const scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(42,canvas.clientWidth/canvas.clientHeight,.1,100),renderer=new THREE.WebGLRenderer({canvas,alpha:true,antialias:true});renderer.setPixelRatio(Math.min(devicePixelRatio,1.8));renderer.setSize(canvas.clientWidth,canvas.clientHeight);camera.position.z=4.6;const globe=new THREE.Mesh(new THREE.SphereGeometry(1.55,54,54),new THREE.MeshBasicMaterial({color:0x0b0d12,wireframe:true,transparent:true,opacity:.33}));scene.add(globe);const glow=new THREE.Mesh(new THREE.SphereGeometry(1.62,54,54),new THREE.MeshBasicMaterial({color:0xff4325,transparent:true,opacity:.035,side:THREE.BackSide}));scene.add(glow);const dots=[];[[.2,.8,1.2],[-1,.5,.8],[1.1,.2,.7],[-.5,-.8,1.1],[.8,-.6,.9]].forEach(p=>{const m=new THREE.Mesh(new THREE.SphereGeometry(.035,12,12),new THREE.MeshBasicMaterial({color:0xff5b3d}));m.position.set(...p);scene.add(m);dots.push(m)});function tick(){requestAnimationFrame(tick);globe.rotation.y+=.0025;glow.rotation.y+=.0025;dots.forEach((d,i)=>d.scale.setScalar(1+Math.sin(performance.now()*.003+i)*.25));renderer.render(scene,camera)}tick();new ResizeObserver(()=>{const w=canvas.clientWidth,h=canvas.clientHeight;camera.aspect=w/h;camera.updateProjectionMatrix();renderer.setSize(w,h)}).observe(canvas)}initBackground();initGlobe();